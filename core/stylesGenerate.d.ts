import { ContentStyleType, PanStyleType } from './types';
export declare const getContentStyle: ({ position, className, }: ContentStyleType) => string;
export declare const getPanStyle: ({ position, className, backgroundColor, }: PanStyleType) => string;
